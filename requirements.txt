# Scientific Document Processing Pipelines - Python Dependencies

# Core data handling
pandas>=2.0.0
openpyxl>=3.1.0  # For Excel file reading

# PPTX extraction
python-pptx>=0.6.21

# PowerPoint COM automation for PPTX slide rendering (Windows only)
pywin32>=306; sys_platform == "win32"

# PDF text extraction (native)
pdfplumber>=0.10.0

# PDF manipulation and figure extraction
PyMuPDF>=1.23.0  # Also known as 'fitz'

# OCR support (optional, for scanned PDFs)
pdf2image>=1.16.0
pytesseract>=0.3.10
Pillow>=10.0.0

# Optional: Vision LLM integration for figure analysis
# anthropic>=0.18.0
# openai>=1.0.0

# Optional: Advanced NLP for better section parsing
# spacy>=3.7.0
# transformers>=4.35.0

# Utility
pathlib2>=2.3.7; python_version < '3.4'
typing-extensions>=4.0.0
